name: CI/CD for ManagerApp Service

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Run docker-compose
        uses: hoverkraft-tech/compose-action@v2.0.1
        with:
          compose-file: "./docker-compose.yml"

      - name: List running containers
        run: |
          echo "Running containers:"
          docker ps -a  # Вывод всех контейнеров

      - name: Wait for services to be ready
        run: |
          sleep 10

      - name: Test connection between services
        run: |
          docker exec PAD_project_deploy_managerapp_1 ping -c 3 PAD_project_deploy_calculator_1 || exit 1
          docker exec PAD_project_deploy_managerapp_1 ping -c 3 PAD_project_deploy_webscrapper_1 || exit 1
          docker exec PAD_project_deploy_managerapp_1 ping -c 3 PAD_project_deploy_mongo_1 || exit 1
          docker exec PAD_project_deploy_managerapp_1 ping -c 3 PAD_project_deploy_mongo-express_1 || exit 1

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build and push Docker image
        run: |
          docker build -t ${{ secrets.DOCKER_USERNAME }}/front:latest ./Front
          docker push ${{ secrets.DOCKER_USERNAME }}/front:latest

      - name: Cleanup
        run: |
          docker-compose down
